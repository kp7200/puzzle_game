import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF171717); // Primary canvas
  static const Color surface = Color(0xFF0F0F0F); // Deep surface

  // Accents
  static const Color primaryGreen = Color(0xFF3ECF8E); // Brand green
  static const Color interactiveGreen = Color(0xFF00C573); // Links, actions

  // Text
  static const Color textPrimary = Color(0xFFFAFAFA);
  static const Color textSecondary = Color(0xFFB4B4B4);
  static const Color textMuted = Color(0xFF898989);

  // Borders & Dividers
  static const Color divider = Color(0xFF242424);
  static const Color border = Color(0xFF2E2E2E);
  static const Color borderHover = Color(0xFF363636);
  static const Color borderAccent = Color(0x4D3ECF8E); // rgba(62,207,142,0.3)
}
